#!/command/with-contenv sh
# Consume socat's piped output and re-emit each line atomically to the
# container stdout (docker logs), prefixed with an ISO 8601 UTC timestamp.
# Atomic per-line writes are what stop socat and otbr-agent from garbling
# each other's lines in the shared log stream.
exec /command/s6-log -- T 1
