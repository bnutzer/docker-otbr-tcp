#!/command/with-contenv bash

exec &>/var/log/servicebasename

if [[ -z "${OTBR_SERVICEBASENAME:-}" ]] ; then
	exit 0
fi

echo "Updating service basename to '${OTBR_SERVICEBASENAME}' ..."
wrap-ot-ctl ba servicebasename "${OTBR_SERVICEBASENAME}"
